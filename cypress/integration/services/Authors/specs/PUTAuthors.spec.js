import * as PUTAuthors from '../requests/PUTAuthors.request'
import * as GETAuthors from '../requests/GETAuthors.request'

describe('PUT - Authors', () => {
   it('alter a Author', () => {
       GETAuthors.allAuthors().should((resAllBooks) => {
         
       })
   }); 
});